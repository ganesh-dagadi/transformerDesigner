import constants from "./constants"

const calculator = {
    calculateRatedVoltage : function(model){
        //apply changes to model, set the required property and then return the model
        return model
    }
}

export default calculator